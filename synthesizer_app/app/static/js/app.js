/**
 * Mobile Synthesizer — application entry point.
 *
 * This file bootstraps the client-side modules once the DOM is ready.
 * Audio engine, WebSocket client, and UI bindings will be initialised here
 * as they are implemented in Phase 2+.
 */

document.addEventListener("DOMContentLoaded", () => {
  console.log("Mobile Synthesizer — ready");
});
